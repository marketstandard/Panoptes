# Panoptes v2.1 neural environment lock (protocol Phase 9).
#
# Pinned, exact versions of the neural training/inference stack used for the
# architecture pilot (Phase 4) and the frozen 3-seed detector (Phase 5).
# Captured from the project .venv on the RTX 3090 machine.
#
# Runtime hardware/driver (recorded, not pip-installable):
#   GPU:    NVIDIA GeForce RTX 3090, 24576 MiB VRAM
#   Driver: 610.47
#   CUDA:   12.6 (torch built as +cu126)
#   OS:     Windows 11 (win32 10.0.26200); Linux smoke path validated separately
#
# Python: 3.12.10
#
# Install with:
#   pip install -r bench/neural/requirements-neural.lock.txt
# (torch must resolve to the cu126 build; use the PyTorch cu126 index.)

--extra-index-url https://download.pytorch.org/whl/cu126

torch==2.13.0+cu126
transformers==5.15.0
tokenizers==0.22.2
accelerate==1.14.0
safetensors==0.8.0
huggingface_hub==1.27.0
numpy==2.5.2
scipy==1.18.0
scikit-learn==1.9.0
sentencepiece==0.2.2
tiktoken==0.14.0
protobuf==7.35.1
